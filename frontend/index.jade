extends layout

block content
	.container-fluid(ng-controller='HomeController')
		.col-sm-4
			.card
				input(type='text', data-ng-model='playlist')
				p(data-ng-bind='playlist')
			.card
				input(type='text', ng-model='songBox', ng-enter='updateArray()')
				ul(data-ng-repeat='name in names')
					li {{name}}
		.col-sm-8
			p col 2